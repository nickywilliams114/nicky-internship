const Item = (id, title, authorImage, nftImage, nftId, authorId, code) => {
 return (
   <>
    <div>{id} key=item.id </div>
    <div>{title}</div>
    <div>{authorImage}</div>
    <div>{nftImage}</div>
    <div>{nftId}</div>
    <div>{authorId}</div>
    <div>{code}</div>
   </>
   
 );
}

export default Item;
 